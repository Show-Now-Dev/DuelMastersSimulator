# ToDo

## ✅ 完了済み

### ドラッグ＆ドロップ操作
- 選択カードのドラッグ移動（単体・複数・スタック一括）
- ゾーン背景へのドロップ（即時移動）
- 別カードへのドロップによるスタック積み重ね
- モバイルタッチ対応（DragDropTouch polyfill）

### 操作の一貫性改善（タップ/表裏）
- 複数カード選択時のスマートトグル実装
  - タップ：1枚でもアンタップがあれば全タップ → 全タップなら全アンタップ
  - 表裏：1枚でも裏向きがあれば全表向き → 全表向きなら全裏向き

### デッキビルドUI
- 文明フィルタ＋カード名検索付き一覧
- 枚数入力・デッキ保存機能
- カード管理・デッキビルダー・デッキ編集で共通検索モジュール（CardSearchUI）

### 画面遷移と状態保持
- 5画面管理（メニュー / カード登録 / デッキビルダー / カード管理 / デッキ編集）
- 各画面間の相互遷移・データ保持

### スタック表示・操作
- バトルゾーン等での複数カードスタック表示
- 枚数バッジをタップしてスタック選択モーダルを開く
- スタック選択モーダルからの移動・表裏操作

### ツインパクト（両面カード）
- カード登録・解析対応
- カード詳細パネル・モーダルでの両面表示（上下分割レイアウト）

---

## 🔴 High Priority（最優先）

### 1. UNDO機能
- 直前の操作を取り消し可能にする
- reducer ベースで履歴管理（past state stack）

---

### 2. スマホ環境の最終確認
- 実機でレイアウト崩れがないか通し確認
- タッチ操作（選択・ドラッグ・スタックバッジタップ）の実機検証

---

### 3. 公開対応（GitHub Pages）
- 初回利用時の案内（データはローカル保存のみ・エクスポート推奨）表示
- GitHub Actions workflow の追加（自動デプロイ）

---

## 🟡 Medium Priority（重要）

### 4. サブデッキ対応
- デッキビルド時にメイン / 超次元 / GR それぞれ編集可能にする
- ゲーム開始時の初期配置に超次元・GR を反映（ゾーン定義は実装済み）

---

### 5. 対戦モード（上下反転UI）
- 2デッキ選択可能にする
- 画面を上下に分割し、相手側を反転表示（transform）
- GameState の player 構造を複数対応に拡張

---

## 🟢 Low Priority（拡張）

### 6. UX改善
- デッキ共有機能（短いID or テキストエクスポート）
- 確率計算機能（ハンド枚数・特定カードの引ける確率）
- クラウド保存

---

## 📝 Notes

- UI / ViewModel / State の責務分離を維持すること
- storage 層は将来的に API へ差し替え可能にしておくこと
- モーダルは共通コンポーネントとして使い回すこと
